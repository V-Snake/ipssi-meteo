# Configuration pour le backend KiteSurf Dashboard
# Copiez ce fichier vers config.env et renseignez vos vraies valeurs

# Configuration Kafka (si utilisée)
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Clé API météo (optionnel, le backend fonctionne avec des mocks)
WEATHER_API_KEY=your_weather_api_key_here

# Configuration du serveur
DEBUG=true
PORT=8001